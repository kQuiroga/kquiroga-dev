---
import SectionHeading from "../ui/SectionHeading.astro";
import type { TranslationShape } from '../../i18n/en';

interface Props {
  t: TranslationShape['about'];
}

const { t } = Astro.props;
---

<section id="about" aria-labelledby="about-heading" class="reveal bg-neutral-50 px-6 py-24">
  <div class="mx-auto max-w-5xl">
    <SectionHeading id="about" title={t.heading} />
    <div class="max-w-2xl space-y-4">
      <p class="text-base sm:text-lg leading-relaxed text-neutral-600">
        {t.p1}
      </p>
      <p class="text-base sm:text-lg leading-relaxed text-neutral-600">
        {t.p2}
      </p>
    </div>
  </div>
</section>
